# Public test endpoints
test-health
testPing

# Core Beacon endpoints (require auth)
datasets auth
datasets/summary auth
individuals auth
individuals/summary auth
runs auth
runs/summary auth
cohorts auth
cohorts/summary auth


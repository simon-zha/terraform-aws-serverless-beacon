Manual develop deployment has been temporarily removed for testing of the automatic environment controller.
This file is used to trigger the develop workflow.
Now it's running well, will keep on working these.